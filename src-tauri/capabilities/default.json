{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal permissions for the main offline window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
